function SyncButton() {
    return (
        <div style={{
            'margin-left': '5vw'
        }}>
            <button class="btn btn-default">Sync</button>
        </div>
    );
}

export default SyncButton