function SyncButton() {
    return (
        <div style={{
            marginLeft: '5vw'
        }}>
            <button className="btn btn-default">Sync</button>
        </div>
    );
}

export default SyncButton